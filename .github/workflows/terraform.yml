name: Terraform

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

env:
  AWS_REGION: ap-southeast-1

jobs:

  ############################################################
  # BOOTSTRAP BACKEND
  ############################################################
  bootstrap:
    name: Bootstrap Terraform Backend
    runs-on: ubuntu-latest
    environment: production

    defaults:
      run:
        working-directory: terraform_bootstrap

    steps:
      ########################################################
      # CHECKOUT
      ########################################################
      - name: Checkout Repository
        uses: actions/checkout@v4

      ########################################################
      # SANITIZE BACKEND NAMES
      ########################################################
      - name: Generate Backend Names
        run: |
          REPO_NAME=$(echo "${{ github.event.repository.name }}" | tr '[:upper:]' '[:lower:]' | tr '_' '-')

          echo "TF_BUCKET=${REPO_NAME}-terraform-state" >> $GITHUB_ENV
          echo "TF_LOCK_TABLE=${REPO_NAME}-terraform-lock" >> $GITHUB_ENV

      ########################################################
      # TERRAFORM
      ########################################################
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      ########################################################
      # AWS AUTH
      ########################################################
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      ########################################################
      # CHECK IF BACKEND EXISTS
      ########################################################
      - name: Check Backend Resources
        id: backend-check
        run: |
          echo "Checking S3 bucket: $TF_BUCKET"

          if aws s3api head-bucket --bucket "$TF_BUCKET" 2>/dev/null; then
            echo "bucket_exists=true" >> $GITHUB_OUTPUT
          else
            echo "bucket_exists=false" >> $GITHUB_OUTPUT
          fi

          echo "Checking DynamoDB table: $TF_LOCK_TABLE"

          if aws dynamodb describe-table \
            --table-name "$TF_LOCK_TABLE" \
            --region "$AWS_REGION" \
            >/dev/null 2>&1; then
            echo "table_exists=true" >> $GITHUB_OUTPUT
          else
            echo "table_exists=false" >> $GITHUB_OUTPUT
          fi

      ########################################################
      # CREATE BACKEND IF MISSING
      ########################################################
      - name: Terraform Init
        if: steps.backend-check.outputs.bucket_exists == 'false' || steps.backend-check.outputs.table_exists == 'false'
        run: terraform init

      - name: Terraform Apply Bootstrap
        if: steps.backend-check.outputs.bucket_exists == 'false' || steps.backend-check.outputs.table_exists == 'false'
        run: |
          terraform apply -auto-approve -input=false \
            -var="bucket_name=$TF_BUCKET" \
            -var="lock_table_name=$TF_LOCK_TABLE"

  ############################################################
  # MAIN TERRAFORM DEPLOYMENT
  ############################################################
  terraform:
    name: Deploy AWS Infrastructure
    runs-on: ubuntu-latest
    needs: bootstrap
    environment: production

    defaults:
      run:
        working-directory: aws_resources

    steps:
      ########################################################
      # CHECKOUT
      ########################################################
      - name: Checkout Repository
        uses: actions/checkout@v4

      ########################################################
      # SANITIZE BACKEND NAMES
      ########################################################
      - name: Generate Backend Names
        run: |
          REPO_NAME=$(echo "${{ github.event.repository.name }}" | tr '[:upper:]' '[:lower:]' | tr '_' '-')

          echo "TF_BUCKET=${REPO_NAME}-terraform-state" >> $GITHUB_ENV
          echo "TF_LOCK_TABLE=${REPO_NAME}-terraform-lock" >> $GITHUB_ENV

      ########################################################
      # TERRAFORM
      ########################################################
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      ########################################################
      # AWS AUTH
      ########################################################
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      ########################################################
      # DEBUG
      ########################################################
      - name: Debug Folder Structure
        run: |
          pwd
          ls -la
          ls -R

      ########################################################
      # FORMAT CHECK
      ########################################################
      - name: Terraform Format Check
        run: terraform fmt -check

      ########################################################
      # TERRAFORM INIT
      ########################################################
      - name: Terraform Init
        run: |
          terraform init \
            -backend-config="bucket=$TF_BUCKET" \
            -backend-config="key=${{ github.ref_name }}/terraform.tfstate" \
            -backend-config="region=$AWS_REGION" \
            -backend-config="dynamodb_table=$TF_LOCK_TABLE"

      ########################################################
      # VALIDATE
      ########################################################
      - name: Terraform Validate
        run: terraform validate

      ########################################################
      # PLAN
      ########################################################
      - name: Terraform Plan
        run: |
          terraform plan \
            -input=false \
            -out=tfplan

      ########################################################
      # APPLY
      ########################################################
      - name: Terraform Apply
        if: github.ref == 'refs/heads/main'
        run: terraform apply -auto-approve tfplan